apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingress-directory.fullname" . }}-test-connection"
  labels:
    {{- include "ingress-directory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ingress-directory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
